import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('users',{id:text('id').primaryKey(),firstName:text('first_name').notNull(),business:text('business').notNull(),phone:text('phone').notNull(),email:text('email').notNull().unique(),passwordHash:text('password_hash').notNull(),role:text('role').notNull().default('user'),createdAt:text('created_at').notNull()});
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),expires:integer('expires').notNull()},t=>[index('idx_sessions_user').on(t.userId)]);
export const slips=sqliteTable('slips',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),supplier:text('supplier').notNull(),date:text('date').notNull(),receiptNumber:text('receipt_number').notNull().default(''),category:text('category').notNull(),totalCents:integer('total_cents').notNull(),vatCents:integer('vat_cents'),fileKey:text('file_key').notNull(),fileName:text('file_name').notNull(),fileType:text('file_type').notNull(),createdAt:text('created_at').notNull()},t=>[index('idx_slips_user_date').on(t.userId,t.date)]);
export const rateLimits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),resetAt:integer('reset_at').notNull()});
